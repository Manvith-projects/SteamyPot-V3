# AI Review Summarizer — Dependencies
# =====================================
# RAG pipeline: MongoDB → Preprocess → Embed → FAISS → LLM → Summary

# Web framework
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.5.0

# MongoDB driver
pymongo>=4.6.0
dnspython>=2.4.0          # Required for mongodb+srv:// URIs

# LangChain (RAG orchestration)
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-google-genai>=2.0.0
langchain-huggingface>=0.1.0

# Google Generative AI (LLM only)
google-generativeai>=0.8.0

# HuggingFace embeddings — free, local, no API key
sentence-transformers>=2.2.0

# Vector store
faiss-cpu>=1.7.4           # FAISS for fast approximate nearest neighbor search

# Utilities (likely already installed)
numpy>=1.24.0

# Optional: expose local API over internet during testing only
pyngrok>=7.1.0
